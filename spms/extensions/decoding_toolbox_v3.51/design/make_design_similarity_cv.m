% function design = make_design_similarity_cv(cfg)
%
% Function to create a design for a similarity analysis. The function calls
% make_design_rsa (representational similarity analysis).
%
% See also MAKE_DESIGN_RSA_CV



function design = make_design_similarity_cv(cfg)

design = make_design_rsa_cv(cfg);